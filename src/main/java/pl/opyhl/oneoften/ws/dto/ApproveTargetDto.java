package pl.opyhl.oneoften.ws.dto;
public class ApproveTargetDto {
    private boolean accept;
    public boolean isAccept(){ return accept; }
    public void setAccept(boolean accept){ this.accept = accept; }
}
